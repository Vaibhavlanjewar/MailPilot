# Dev: Vite proxies /api to the backend (see vite.config.js)
VITE_API_URL=/api

# Optional: if you open the client on another origin, set full API URL + CORS on server
# VITE_API_URL=http://localhost:4000/api
